build:
    tests:
        override:
            -
                command: 'py.test --cov trains'
                coverage:
                    file: '.coverage'
                    config_file: '.coveragerc'
                    format: 'py-cc'
